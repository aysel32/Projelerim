
import random


my_list = ["Aysel", "Cengiz", "Emine", "Filiz", "Gülsüm", "Gülşah", "Keziban", "Onur-H", "Şerife", "Zeynep"]
random.shuffle(my_list)

for ix, eleman in enumerate(my_list,1):
    print(f"{ix} => {eleman}")
